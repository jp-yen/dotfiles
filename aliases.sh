#!/bin/sh
# /etc/profile.d/aliases.sh
# sh / bash / ash 共通エイリアス

alias c='clear'
alias JP='export LANG=ja_JP.utf8'
alias C='unset LANG LC_ALL'

alias ls='ls -F --color=tty'
alias ll='ls -l --color=tty'
alias screen ="/usr/bin/screen -U -D -RR"

